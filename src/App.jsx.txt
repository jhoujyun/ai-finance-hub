import React, { useState, useEffect } from 'react';
import { TrendingUp, Calculator, Calendar, Newspaper, Menu, X, Sparkles, RefreshCw, ExternalLink } from 'lucide-react';

function App() {
  const [activeTab, setActiveTab] = useState('news');
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [newsData, setNewsData] = useState([]);
  const [newsLoading, setNewsLoading] = useState(false);
  const [newsError, setNewsError] = useState(null);
  const [economicCalendar, setEconomicCalendar] = useState([]);
  const [calendarLoading, setCalendarLoading] = useState(false);
  
  const [compoundInterest, setCompoundInterest] = useState({
    principal: 100000,
    rate: 7,
    years: 10,
    frequency: 12
  });
  
  const [retirementCalc, setRetirementCalc] = useState({
    currentAge: 30,
    retireAge: 60,
    monthlyExpense: 30000,
    currentSavings: 500000,
    monthlyContribution: 10000,
    returnRate: 9
  });

  const fetchNews = async () => {
    setNewsLoading(true);
    setNewsError(null);
    
    try {
      const response = await fetch('/api/news');
      const data = await response.json();
      
      if (data.success) {
        setNewsData(data.news);
      } else {
        throw new Error(data.error || '抓取新聞失敗');
      }
    } catch (error) {
      console.error('新聞抓取錯誤:', error);
      setNewsError(error.message);
      setNewsData([
        {
          id: 1,
          title: "歡迎使用 AI 財經工具站",
          source: "系統訊息",
          time: "現在",
          summary: "請確認 API 設定正確以獲取即時財經新聞。",
          aiInsight: "💡 提示：檢查 Vercel 環境變數是否已正確設定 NEWS_API_KEY 和 ANTHROPIC_API_KEY",
          category: "系統訊息"
        }
      ]);
    } finally {
      setNewsLoading(false);
    }
  };

  const fetchCalendar = async () => {
    setCalendarLoading(true);
    
    try {
      const response = await fetch('/api/calendar');
      const data = await response.json();
      
      if (data.success) {
        setEconomicCalendar(data.events);
      }
    } catch (error) {
      console.error('經濟日曆抓取錯誤:', error);
      // 使用預設數據
      const today = new Date();
      setEconomicCalendar([
        {
          date: new Date(today.getTime() + 0 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
          event: "美國初領失業金人數",
          importance: "high",
          previous: "21.2萬",
          forecast: "22萬",
          aiAnalysis: "若數據高於預期，可能強化聯準會鴿派立場，利好股市；低於預期則相反。"
        },
        {
          date: new Date(today.getTime() + 2 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
          event: "美國 Q4 GDP 初值",
          importance: "high",
          previous: "3.1%",
          forecast: "2.8%",
          aiAnalysis: "GDP 放緩符合軟著陸預期，但若大幅低於 2.5% 可能引發衰退擔憂。"
        },
        {
          date: new Date(today.getTime() + 3 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
          event: "歐洲央行利率決議",
          importance: "high",
          previous: "3.25%",
          forecast: "3.00%",
          aiAnalysis: "市場已計價降息 25 基點，關注拉加德對未來政策路徑的表態。"
        },
        {
          date: new Date(today.getTime() + 5 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
          event: "中國 1 月官方 PMI",
          importance: "medium",
          previous: "50.1",
          forecast: "50.3",
          aiAnalysis: "春節因素可能影響數據，需結合財新 PMI 綜合判斷經濟動能。"
        }
      ]);
    } finally {
      setCalendarLoading(false);
    }
  };

  useEffect(() => {
    fetchNews();
    fetchCalendar();
  }, []);

  const calculateCompoundInterest = () => {
    const { principal, rate, years, frequency } = compoundInterest;
    const r = rate / 100;
    const n = frequency;
    const t = years;
    const amount = principal * Math.pow((1 + r / n), n * t);
    const interest = amount - principal;
    return { amount, interest };
  };

  const calculateRetirement = () => {
    const { currentAge, retireAge, monthlyExpense, currentSavings, monthlyContribution, returnRate } = retirementCalc;
    const yearsToRetire = retireAge - currentAge;
    const monthlyRate = returnRate / 100 / 12;
    
    const futureValue = currentSavings * Math.pow(1 + returnRate / 100, yearsToRetire) +
      monthlyContribution * ((Math.pow(1 + monthlyRate, yearsToRetire * 12) - 1) / monthlyRate);
    
    const yearsInRetirement = 25;
    const sustainableMonthly = futureValue / (yearsInRetirement * 12);
    
    return {
      futureValue,
      sustainableMonthly,
      shortfall: monthlyExpense - sustainableMonthly
    };
  };

  const compoundResult = calculateCompoundInterest();
  const retirementResult = calculateRetirement();

  const tabs = [
    { id: 'news', label: 'AI 新聞摘要', icon: Newspaper },
    { id: 'calculator', label: '財務計算器', icon: Calculator },
    { id: 'calendar', label: '經濟日曆', icon: Calendar }
  ];

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50">
      <nav className="bg-white shadow-sm border-b sticky top-0 z-50">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between items-center h-16">
            <div className="flex items-center space-x-3">
              <img src="/logo.svg" alt="AI 財經工具站 Logo" className="w-10 h-10" />
              <div>
                <h1 className="text-xl font-bold text-gray-900">AI 財經工具站</h1>
                <p className="text-xs text-gray-500">智能分析 · 精準決策</p>
              </div>
            </div>
            
            <div className="hidden md:flex space-x-1">
              {tabs.map(tab => (
                <button
                  key={tab.id}
                  onClick={() => setActiveTab(tab.id)}
                  className={`px-4 py-2 rounded-lg flex items-center space-x-2 transition ${
                    activeTab === tab.id
                      ? 'bg-blue-600 text-white'
                      : 'text-gray-600 hover:bg-gray-100'
                  }`}
                >
                  <tab.icon className="w-5 h-5" />
                  <span>{tab.label}</span>
                </button>
              ))}
            </div>

            <button 
              className="md:hidden"
              onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
            >
              {mobileMenuOpen ? <X className="w-6 h-6" /> : <Menu className="w-6 h-6" />}
            </button>
          </div>

          {mobileMenuOpen && (
            <div className="md:hidden pb-4 space-y-2">
              {tabs.map(tab => (
                <button
                  key={tab.id}
                  onClick={() => {
                    setActiveTab(tab.id);
                    setMobileMenuOpen(false);
                  }}
                  className={`w-full px-4 py-2 rounded-lg flex items-center space-x-2 transition ${
                    activeTab === tab.id
                      ? 'bg-blue-600 text-white'
                      : 'text-gray-600 hover:bg-gray-100'
                  }`}
                >
                  <tab.icon className="w-5 h-5" />
                  <span>{tab.label}</span>
                </button>
              ))}
            </div>
          )}
        </div>
      </nav>

      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {activeTab === 'news' && (
          <div className="space-y-6">
            <div className="flex items-center justify-between mb-6">
              <div className="flex items-center space-x-2">
                <Sparkles className="w-6 h-6 text-yellow-500" />
                <h2 className="text-2xl font-bold text-gray-900">AI 全球財經新聞</h2>
              </div>
              <button
                onClick={fetchNews}
                disabled={newsLoading}
                className="flex items-center space-x-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition disabled:opacity-50"
              >
                <RefreshCw className={`w-4 h-4 ${newsLoading ? 'animate-spin' : ''}`} />
                <span>{newsLoading ? '載入中...' : '重新整理'}</span>
              </button>
            </div>

            {newsError && (
              <div className="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded">
                <p className="text-yellow-700">⚠️ {newsError}</p>
                <p className="text-sm text-yellow-600 mt-1">請檢查 API 設定</p>
              </div>
            )}
            
            {newsData.map(news => (
              <div key={news.id} className="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                <div className="flex items-start justify-between mb-3">
                  <div className="flex-1">
                    <div className="flex items-center space-x-2 mb-2">
                      <span className="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-sm font-medium">
                        {news.category}
                      </span>
                      <span className="text-sm text-gray-500">{news.source} · {news.time}</span>
                    </div>
                    <h3 className="text-xl font-semibold text-gray-900 mb-2">{news.title}</h3>
                    <p className="text-gray-600 mb-4">{news.summary}</p>
                  </div>
                </div>
                
                <div className="bg-gradient-to-r from-purple-50 to-blue-50 rounded-lg p-4 border-l-4 border-purple-500 mb-3">
                  <div className="flex items-start space-x-2">
                    <Sparkles className="w-5 h-5 text-purple-600 mt-0.5 flex-shrink-0" />
                    <p className="text-gray-700">{news.aiInsight}</p>
                  </div>
                </div>

                {news.url && (
                  <a
                    href={news.url}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="inline-flex items-center space-x-1 text-blue-600 hover:text-blue-700 text-sm"
                  >
                    <span>閱讀原文</span>
                    <ExternalLink className="w-4 h-4" />
                  </a>
                )}
              </div>
            ))}

            <div className="bg-gray-100 rounded-xl p-8 text-center border-2 border-dashed border-gray-300">
              <p className="text-gray-500">Google AdSense 廣告位</p>
            </div>
          </div>
        )}

        {activeTab === 'calculator' && (
          <div className="space-y-8">
            <h2 className="text-2xl font-bold text-gray-900 mb-6">財務計算工具</h2>
            
            <div className="bg-white rounded-xl shadow-md p-6">
              <h3 className="text-xl font-semibold text-gray-900 mb-4">複利計算器</h3>
              <div className="grid md:grid-cols-2 gap-6">
                <div className="space-y-4">
                  <div>
                    <label className="block text-sm font-medium text-gray-700 mb-2">初始本金 (NT$)</label>
                    <input
                      type="number"
                      value={compoundInterest.principal}
                      onChange={(e) => setCompoundInterest({...compoundInterest, principal: Number(e.target.value)})}
                      className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-gray-700 mb-2">年利率 (%)</label>
                    <input
                      type="number"
                      value={compoundInterest.rate}
                      onChange={(e) => setCompoundInterest({...compoundInterest, rate: Number(e.target.value)})}
                      className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-gray-700 mb-2">投資年限</label>
                    <input
                      type="number"
                      value={compoundInterest.years}
                      onChange={(e) => setCompoundInterest({...compoundInterest, years: Number(e.target.value)})}
                      className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-gray-700 mb-2">複利頻率</label>
                    <select
                      value={compoundInterest.frequency}
                      onChange={(e) => setCompoundInterest({...compoundInterest, frequency: Number(e.target.value)})}
                      className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    >
                      <option value={1}>年</option>
                      <option value={4}>季</option>
                      <option value={12}>月</option>
                      <option value={365}>日</option>
                    </select>
                  </div>
                </div>
                
                <div className="bg-gradient-to-br from-green-50 to-emerald-50 rounded-lg p-6">
                  <h4 className="text-lg font-semibold text-gray-900 mb-4">計算結果</h4>
                  <div className="space-y-3">
                    <div className="flex justify-between items-center">
                      <span className="text-gray-600">最終金額</span>
                      <span className="text-2xl font-bold text-green-600">
                        NT$ {compoundResult.amount.toLocaleString('zh-TW', {maximumFractionDigits: 0})}
                      </span>
                    </div>
                    <div className="flex justify-between items-center">
                      <span className="text-gray-600">累積利息</span>
                      <span className="text-xl font-semibold text-emerald-600">
                        NT$ {compoundResult.interest.toLocaleString('zh-TW', {maximumFractionDigits: 0})}
                      </span>
                    </div>
                    <div className="flex justify-between items-center pt-3 border-t">
                      <span className="text-gray-600">報酬率</span>
                      <span className="text-lg font-medium text-gray-900">
                        {((compoundResult.interest / compoundInterest.principal) * 100).toFixed(1)}%
                      </span>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <div className="bg-white rounded-xl shadow-md p-6">
              <h3 className="text-xl font-semibold text-gray-900 mb-4">退休規劃計算器</h3>
              <div className="grid md:grid-cols-2 gap-6">
                <div className="space-y-4">
                  <div className="grid grid-cols-2 gap-4">
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">目前年齡</label>
                      <input
                        type="number"
                        value={retirementCalc.currentAge}
                        onChange={(e) => setRetirementCalc({...retirementCalc, currentAge: Number(e.target.value)})}
                        className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                      />
                    </div>
                    <div>
                      <label className="block text-sm font-medium text-gray-700 mb-2">退休年齡</label>
                      <input
                        type="number"
                        value={retirementCalc.retireAge}
                        onChange={(e) => setRetirementCalc({...retirementCalc, retireAge: Number(e.target.value)})}
                        className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                      />
                    </div>
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-gray-700 mb-2">退休後月支出 (NT$)</label>
                    <input
                      type="number"
                      value={retirementCalc.monthlyExpense}
                      onChange={(e) => setRetirementCalc({...retirementCalc, monthlyExpense: Number(e.target.value)})}
                      className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-gray-700 mb-2">目前儲蓄 (NT$)</label>
                    <input
                      type="number"
                      value={retirementCalc.currentSavings}
                      onChange={(e) => setRetirementCalc({...retirementCalc, currentSavings: Number(e.target.value)})}
                      className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-gray-700 mb-2">每月投入 (NT$)</label>
                    <input
                      type="number"
                      value={retirementCalc.monthlyContribution}
                      onChange={(e) => setRetirementCalc({...retirementCalc, monthlyContribution: Number(e.target.value)})}
                      className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                  <div>
                    <label className="block text-sm font-medium text-gray-700 mb-2">預期年報酬率 (%)</label>
                    <input
                      type="number"
                      value={retirementCalc.returnRate}
                      onChange={(e) => setRetirementCalc({...retirementCalc, returnRate: Number(e.target.value)})}
                      className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                </div>
                
                <div className="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-lg p-6">
                  <h4 className="text-lg font-semibold text-gray-900 mb-4">退休分析</h4>
                  <div className="space-y-4">
                    <div>
                      <p className="text-sm text-gray-600 mb-1">退休時預計資產</p>
                      <p className="text-2xl font-bold text-blue-600">
                        NT$ {retirementResult.futureValue.toLocaleString('zh-TW', {maximumFractionDigits: 0})}
                      </p>
                    </div>
                    <div>
                      <p className="text-sm text-gray-600 mb-1">可支撐月支出</p>
                      <p className="text-xl font-semibold text-indigo-600">
                        NT$ {retirementResult.sustainableMonthly.toLocaleString('zh-TW', {maximumFractionDigits: 0})}
                      </p>
                    </div>
                    <div className={`p-4 rounded-lg ${retirementResult.shortfall > 0 ? 'bg-red-100' : 'bg-green-100'}`}>
                      <p className="text-sm font-medium mb-1">
                        {retirementResult.shortfall > 0 ? '⚠️ 資金缺口' : '✅ 目標達成'}
                      </p>
                      <p className={`text-lg font-bold ${retirementResult.shortfall > 0 ? 'text-red-700' : 'text-green-700'}`}>
                        {retirementResult.shortfall > 0 
                          ? `每月短缺 NT$ ${retirementResult.shortfall.toLocaleString('zh-TW', {maximumFractionDigits: 0})}`
                          : `每月盈餘 NT$ ${Math.abs(retirementResult.shortfall).toLocaleString('zh-TW', {maximumFractionDigits: 0})}`
                        }
                      </p>
                    </div>
                    <p className="text-xs text-gray-500 pt-2">
                      * 假設退休後存活 25 年，未考慮通脹因素
                    </p>
                  </div>
                </div>
              </div>
            </div>

            <div className="bg-gray-100 rounded-xl p-8 text-center border-2 border-dashed border-gray-300">
              <p className="text-gray-500">Google AdSense 廣告位</p>
            </div>
          </div>
        )}

        {activeTab === 'calendar' && (
          <div className="space-y-6">
            <div className="flex items-center justify-between mb-6">
              <div className="flex items-center space-x-2">
                <Calendar className="w-6 h-6 text-blue-600" />
                <h2 className="text-2xl font-bold text-gray-900">全球經濟日曆</h2>
              </div>
              <button
                onClick={fetchCalendar}
                disabled={calendarLoading}
                className="flex items-center space-x-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition disabled:opacity-50"
              >
                <RefreshCw className={`w-4 h-4 ${calendarLoading ? 'animate-spin' : ''}`} />
                <span>{calendarLoading ? '載入中...' : '重新整理'}</span>
              </button>
            </div>
            
            <div className="space-y-4">
              {economicCalendar.map((event, index) => (
                <div key={index} className="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                  <div className="flex items-start justify-between mb-4">
                    <div className="flex-1">
                      <div className="flex items-center space-x-3 mb-2">
                        <span className="text-lg font-semibold text-gray-900">
                          {new Date(event.date).toLocaleDateString('zh-TW', { month: 'long', day: 'numeric', weekday: 'short' })}
                        </span>
                        <span className={`px-3 py-1 rounded-full text-xs font-medium ${
                          event.importance === 'high' 
                            ? 'bg-red-100 text-red-700' 
                            : 'bg-yellow-100 text-yellow-700'
                        }`}>
                          {event.importance === 'high' ? '高重要性' : '中重要性'}
                        </span>
                      </div>
                      <h3 className="text-xl font-semibold text-gray-900 mb-3">{event.event}</h3>
                      
                      <div className="flex space-x-6 text-sm mb-4">
                        <div>
                          <span className="text-gray-500">前值：</span>
                          <span className="font-medium text-gray-900">{event.previous}</span>
                        </div>
                        <div>
                          <span className="text-gray-500">預測：</span>
                          <span className="font-medium text-gray-900">{event.forecast}</span>
                        </div>
                      </div>
                    </div>
                  </div>
                  
                  <div className="bg-gradient-to-r from-blue-50 to-cyan-50 rounded-lg p-4 border-l-4 border-blue-500">
                    <div className="flex items-start space-x-2">
                      <Sparkles className="w-5 h-5 text-blue-600 mt-0.5 flex-shrink-0" />
                      <div>
                        <p className="text-sm font-medium text-gray-900 mb-1">AI 影響分析</p>
                        <p className="text-gray-700">{event.aiAnalysis}</p>
                      </div>
                    </div>
                  </div>
                </div>
              ))}
            </div>

            <div className="bg-gray-100 rounded-xl p-8 text-center border-2 border-dashed border-gray-300">
              <p className="text-gray-500">Google AdSense 廣告位</p>
            </div>
          </div>
        )}
      </main>

      <footer className="bg-white border-t mt-12">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
          <div className="text-center text-gray-600 text-sm">
            <p>© 2025 AI 財經工具站 · Powered by Claude AI</p>
            <p className="mt-2 text-xs text-gray-500">數據僅供參考，投資有風險，決策需謹慎</p>
          </div>
        </div>
      </footer>
    </div>
  );
}

export default App;